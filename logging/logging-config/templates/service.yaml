apiVersion: v1
kind: Service
metadata:
  name: {{ include "logging-config.fullname" . }}
  labels:
    {{- include "logging-config.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "logging-config.selectorLabels" . | nindent 4 }}
