apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "logging-config.fullname" . }}-test-connection"
  labels:
    {{- include "logging-config.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "logging-config.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
